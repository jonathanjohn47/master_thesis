fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
torch>=2.0.0
numpy>=1.24.0
requests>=2.31.0
# AIJack is optional - install separately when needed for attack evaluation
# Note: AIJack requires Boost C++ libraries on Windows, which may require manual setup
# aijack>=0.0.1b2

